

class basemodel:
	
	constructor:
	def __init__ (self, *args, *kwargs):
        id = str(random)
        swtich type:
        case doctor:
            id = 'P' + id
        case manager:
            id = 'M' + id
        case resptionist:
            id = 'R' + id
        default: #nusres is the deafault
            id = 'N' + id
        self.start_date = date.time(today)


# all worker inherts from basemodel
# we gonna use random to generate th id only numbers with fixed digits
